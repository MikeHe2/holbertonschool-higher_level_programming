#!/usr/bin/python3
"""
my_module
---------

This module contains the `Square` class, which defines a square with a
private instance attribute `size`.

"""


class Square:

    """
    Square defines a square with a private instance attribute `size`.

    Attributes:
        __size (int): Size of the square.

    Methods:
        __init__(self, size): Initializes a new instance of the Square class.
    """

    def __init__(self, size):

        """
        Initializes a new instance of the Square class.

        Args:
            size (int): The size of the square.
        """
        self.__size = size

        """
        Inicializa una nueva instancia de la clase Square.

        Args:
            size (int): El tamaño del cuadrado.
        """
        self.__size = size
